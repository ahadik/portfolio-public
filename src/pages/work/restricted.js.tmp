import React from 'react';
import { Router } from "@reach/router";

import PrivateRoute from "~components/PrivateRoute";

const Restricted = () => {
  return(
    <Router basepath="/work/restricted">
      <PrivateRoute path="/datasets" component={() => <p>Hello World</p>} />
      <Login path="/login" />
      <Default path="/" />
    </Router>
  );
}

export default Restricted;
